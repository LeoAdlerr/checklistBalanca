# =======================================================
# Containerfile Multi-Stage para Produção (Frontend)
# Versão corrigida para build com dependências E2E
# =======================================================

# --- ESTÁGIO 1: Build Stage ---
# Usamos a imagem node:20-slim, pois ela é a base para as dependências do Cypress
FROM node:20-slim AS builder

# Define o diretório de trabalho
WORKDIR /app

# Instala as dependências do sistema para o Cypress e Chromium
# Esta é a lista mínima de pacotes que você forneceu, garantindo que tudo funcione.
RUN apt-get update && apt-get install -y \
    wget \
    curl \
    gnupg \
    libgtk-3-0 \
    libnss3 \
    libasound2 \
    libxss1 \
    libx11-xcb1 \
    libxcomposite1 \
    libxdamage1 \
    libxrandr2 \
    libxshmfence1 \
    libgbm1 \
    libxext6 \
    libxfixes3 \
    libxi6 \
    libxcb1 \
    libxtst6 \
    libpangocairo-1.0-0 \
    libatk-bridge2.0-0 \
    libcups2 \
    fonts-liberation \
    libappindicator3-1 \
    libdbus-1-3 \
    xvfb \
    dos2unix \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

# Copia os arquivos de dependências e instala-as
COPY package.json yarn.lock ./
RUN yarn install --frozen-lockfile

# Copia o restante do código da aplicação
COPY . .

# Converte e garante permissão nos scripts
RUN dos2unix ./entrypoint.sh ./wait-for-it.sh && \
    chmod +x ./entrypoint.sh ./wait-for-it.sh

# --- Etapas de Teste antes da Build ---
# Agora, vamos rodar os testes unitários e E2E.
# O `wait-for-it.sh` não pode ser usado aqui, pois a build é isolada.
# Por isso, vamos assumir que o backend é um mock ou que a sua pipeline de CI/CD
# garante que esses testes rodem em um ambiente adequado.
RUN echo "🧪 Frontend: Executando testes unitários..." && yarn test:unit
RUN echo "🚀 Frontend: Executando testes E2E..." && yarn test:e2e

# Executa o script de build para gerar a pasta /dist
RUN echo "🏗️ Frontend: Gerando build de produção..." && yarn build


# --- ESTÁGIO 2: Production Stage ---
# Começamos com uma imagem nova e super leve do Nginx
FROM nginx:1.25-alpine

# Remove a página de boas-vindas padrão do Nginx
RUN rm -rf /usr/share/nginx/html/*

# Copia a configuração customizada do Nginx
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Copia os ficheiros estáticos construídos no estágio 'builder'
COPY --from=builder /app/dist /usr/share/nginx/html

# Expõe a porta 80, que é a porta padrão do Nginx
EXPOSE 80

# Comando para iniciar o Nginx.
CMD ["nginx", "-g", "daemon off;"]